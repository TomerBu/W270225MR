chat_people = ['Tamir', 'Nina', 'Raz', 'Tamir', 'Doron']

# convert list to set:
chat_set = set(chat_people)

# convert set to list:
chat_list = list(chat_set)

# one liner to remove duplicates
chat_people = list(set(chat_people))

